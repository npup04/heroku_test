<%= form_for(@heroku_test_app) do |f| %>
  <% if @heroku_test_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heroku_test_app.errors.count, "error") %> prohibited this heroku_test_app from being saved:</h2>

      <ul>
      <% @heroku_test_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
